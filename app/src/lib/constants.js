export const DEFAULT_NODE_RADIUS = 3;
export const MAX_SELECTED_NODES = 20;
export const ENCOUNTER_BUCKET_SECONDS = 3600; // 1 hour — keeps the Encounters menu's group count manageable

// Reserved color for message carriers that aren't part of the selected-node set.
// Pure black sits maximally distinct from the saturated hues in NODE_COLOR_PALETTE.
export const CARRIER_RESERVED_COLOR = "#000000";
